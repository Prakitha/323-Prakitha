/*import { useState } from "react";

const Player = (props) => {
    let [state, setScore] = useScore({
        lastScore: 0,
        score: props.score
    })
    f1 = () => {
        let tempScore = Math.floor(Math.random() * 10)
        this.setState({ score: this.state.score + tempScore, lastScore: tempScore })
    }


return (
    <p>
        Name: {props.name}
    </p>
)

}



export default Player;*/